name: R-CMD-check

on:
  pull_request:
    branches: [main, master]

jobs:
  R-CMD-check:
    runs-on: ubuntu-latest
    container: rocker/tidyverse:latest

    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}

    steps:
      - uses: actions/checkout@v3

      - name: Cache renv package cache
        uses: actions/cache@v3
        with:
          path: /root/.local/share/renv/cache
          key: ${{ runner.os }}-renv-${{ hashFiles('**/renv.lock') }}
          restore-keys: |
            ${{ runner.os }}-renv-

      - name: Cache renv library
        uses: actions/cache@v3
        with:
          path: renv/library
          key: ${{ runner.os }}-renv-lib-${{ hashFiles('**/renv.lock') }}
          restore-keys: |
            ${{ runner.os }}-renv-lib-

      - name: Install renv and restore
        run: |
          install.packages("renv")
          renv::restore(prompt = FALSE)
        shell: Rscript {0}

      - name: Check package
        run: |
          devtools::check(error_on = "warning", manual = FALSE, vignettes = FALSE)
        shell: Rscript {0}
