# Extract field definitions from JSON schema

Extract field definitions from JSON schema

## Usage

``` r
extract_fields_from_json_schema(schema_json_list)
```

## Arguments

- schema_json_list:

  Parsed JSON schema as list

## Value

Named list with field names, types, and requirements
