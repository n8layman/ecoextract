# Generate SQL column definitions from JSON schema

Generate SQL column definitions from JSON schema

## Usage

``` r
generate_columns_from_json_schema(schema_json_list)
```

## Arguments

- schema_json_list:

  Parsed JSON schema as list

## Value

Character string with SQL column definitions
