# Generate a UUID v4 identifier

Produces a standards-compliant UUID v4 using base R only (no extra
dependency).

## Usage

``` r
generate_uuid()
```

## Value

Character string in the form "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx"
