# Get extraction context template (internal)

Get extraction context template (internal)

## Usage

``` r
get_extraction_context_template(context_file = NULL)
```

## Arguments

- context_file:

  Optional path to custom context template file

## Value

Character string with context template
