# Internal prompt management functions

Handle system prompts and templates for ecological data extraction Get
extraction prompt from package or custom location (internal)

## Usage

``` r
get_extraction_prompt(prompt_file = NULL)
```

## Arguments

- prompt_file:

  Optional path to custom extraction prompt file

## Value

Character string with extraction prompt
