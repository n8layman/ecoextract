# Get metadata context template (internal)

Get metadata context template (internal)

## Usage

``` r
get_metadata_context(context_file = NULL)
```

## Arguments

- context_file:

  Optional path to custom context template file

## Value

Character string with context template
