# Get record table column definitions as SQL

Get record table column definitions as SQL

## Usage

``` r
get_record_columns_sql(schema_json_list)
```

## Arguments

- schema_json_list:

  Parsed JSON schema to generate columns from

## Value

Character string with column definitions
