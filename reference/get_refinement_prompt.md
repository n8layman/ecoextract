# Get refinement prompt from package or custom location (internal)

Get refinement prompt from package or custom location (internal)

## Usage

``` r
get_refinement_prompt(prompt_file = NULL)
```

## Arguments

- prompt_file:

  Optional path to custom refinement prompt file

## Value

Character string with refinement prompt
