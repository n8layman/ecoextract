# Check if a document should be forced to reprocess

Check if a document should be forced to reprocess

## Usage

``` r
is_forced(force_param, document_id)
```

## Arguments

- force_param:

  NULL, TRUE, or integer vector of document_ids

- document_id:

  The document ID to check

## Value

logical
