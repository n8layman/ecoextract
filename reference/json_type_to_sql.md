# Map a JSON type to an SQLite column type (internal)

Arrays and objects are stored as JSON text.

## Usage

``` r
json_type_to_sql(json_type)
```

## Arguments

- json_type:

  JSON type name

## Value

SQL type string
