# Parse Citation Text to Extract Fields

Attempts to extract structured fields from a citation string using
heuristic pattern matching.

## Usage

``` r
parse_citation_text(citation_text)
```

## Arguments

- citation_text:

  Character string containing citation

## Value

List with extracted fields (author, title, journal, year, etc.)
